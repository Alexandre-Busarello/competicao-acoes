# Feature: Admin

## Escopo (inferido do código)

- Painel admin com estatísticas, gestão de **banners** do feed, broadcast de notificações, etc.

## Implementação (pistas)

- Páginas: `src/app/admin/*`, `src/app/(main)/admin/*`.
- APIs: `src/app/api/admin/*` (stats, banners, notifications broadcast).

## Permissões

- ⚠️ **DECISÃO PENDENTE:** documentar critério exato de quem é admin (env, role, lista fixa) conforme implementação real.
